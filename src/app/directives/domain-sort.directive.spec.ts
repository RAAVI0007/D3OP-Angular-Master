import { DomainSortDirective } from './domain-sort.directive';

describe('DomainSortDirective', () => {
  it('should create an instance', () => {
    const directive = new DomainSortDirective();
    expect(directive).toBeTruthy();
  });
});
