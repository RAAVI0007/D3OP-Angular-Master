import { CompetencyTitlePipe } from './competency-title.pipe';

describe('CompetencyTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CompetencyTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
