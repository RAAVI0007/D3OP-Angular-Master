import { CompetencyDomainPipe } from './competency-domain.pipe';

describe('CompetencyDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new CompetencyDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
